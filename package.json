{
  "name": "voice-clone-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Pages voice cloning and text-to-speech web app.",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-06-09",
    "deploy": "wrangler pages deploy public",
    "check": "wrangler pages functions build --project-directory=."
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
